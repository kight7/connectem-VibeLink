# connectem

Work_Flow/connectem — backend for the Connectem project.

This repository contains the backend for the Connectem project. It's a Python web application FastAPI.

How to use

- Create a virtual environment: `python -m venv venv`
- Activate it: `source venv/bin/activate`
- Install dependencies: `pip install -r backend/requirements.txt` (if present)

License

Specify a license here if you want the repo to be open for contributions.
